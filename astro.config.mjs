// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // site: 'https://blog.pages.dev',  // fill in after first Cloudflare Pages deploy
});
